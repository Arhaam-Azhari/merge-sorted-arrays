CS 151 – Homework 1
Merge Sorted Arrays
# Merge Sorted Arrays

This project implements merging of two sorted arrays using Java.
The project is built using Maven and includes JUnit tests.
## How to Run

```bash
mvn clean test
# Merge Sorted Arrays

This project implements merging of two sorted arrays in Java.

## Structure
- SortedArrayMerger.java — main implementation
- SortedArrayMergerTest.java — JUnit tests

## Build & Test
Run tests using Maven:

mvn test
